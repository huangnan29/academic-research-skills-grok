# 更新记录

## 0.3.1 候选（未发布）— 2026-08-30

- 新增基于init工具表、工具调用回执和文件摘要的运行时验收，不再用预置答案标记证明安全；
- 实测Grok 1.0.13直接Agent启动仍暴露MCP入口，明确保留能力限制；
- 修复Hook把项目根目录与子目录cwd当成冲突的问题，并保护Grok自身守卫与Agent文件；
- 支持终端内部工具别名，备份清理只处理带本安装器所有者标记的目录；
- 保留默认关闭Hook和人工确认边界，不把未完成的原生调度标记为成功。

## 0.3.0 — 2026-08-29

- 增加四个命名空间化Grok原生Skill入口；
- 增加Claude原版对应的三个Grok原生Agent；
- 增加默认关闭、显式安装的PreToolUse本地写入范围Hook；
- 记录Grok 1.0.5忽略SessionStart stdout的能力缺口，不交付无效公告Hook；
- 为十三个轻任务命令配置`effort: medium`，三个重任务继续继承当前设置；
- 安装器纳入Agent和可选Hook的备份、幂等与一致性验证；
- 保持上游ARS 3.21.1、跨模型默认关闭和私有材料不外传边界。

## 0.2.1 — 2026-08-29

- 将 Release 附件从 `runtime-minimal` 更名为语义更准确的 `runtime-core`；
- 排除 `ars/scripts/test_*.py`、脚本内部 `tests/` 目录和 CI 清单；
- 排除核心运行不需要的上游顶层 README、CHANGELOG 和开发配置；
- 保留所有非测试运行脚本、fixtures、工作流、角色、参考资料、模板和共享契约；
- 增加 core 包安装器兼容、确定性摘要和运行脚本保留测试；
- 不覆盖历史 `v0.2.0` Release，另行发布 `v0.2.1`。

## 0.2.0 — 2026-08-29

- 将固定上游 ARS 从 3.21.0 升级到正式发行版 3.21.1；
- 增加公开仓库根级持续集成；
- 安装前验证版本、文件数量和 vendored tree SHA-256；
- 相同版本和内容重复安装时不再创建备份；
- 增加备份保留数量控制；
- 增加轻量运行包构建和五类行为契约；
- 保持 Hook、跨模型 API、付费服务和私有材料外传默认关闭。

## 0.1.0 — 2026-08-24

- 首次发布 Grok Build 单技能路由；
- 固定 ARS 3.21.0 与 Experiment Agent 1.1.0；
- 注册 16 个 Grok 原生 `ars-*` 命令；
- 增加安装器、静态验证和 13 项适配器测试；
- 完成 Grok 技能发现和 Socratic 路由冒烟测试。
