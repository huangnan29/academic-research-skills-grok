---
description: 使一条 ARS 引用核验缓存记录失效
argument-hint: 引用键与失效原因
allowed-tools: [read_file, list_dir, grep, run_terminal_command, search_replace]
---

加载并遵循 `academic-research-suite` 根技能。读取 `ars/commands/ars-cache-invalidate.md`，仅处理用户指定的引用键，并保留操作结果：

$ARGUMENTS
