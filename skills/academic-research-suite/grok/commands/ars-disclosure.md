---
description: 使用 ARS-Grok 生成或审查学术 AI 使用披露
argument-hint: 稿件、实际使用方式与期刊政策
allowed-tools: [read_file, list_dir, grep, web_search, search_replace]
effort: medium
---

加载并遵循 `academic-research-suite` 根技能。读取 `ars/commands/ars-disclosure.md` 和 `ars/academic-paper/WORKFLOW.md`，以 `disclosure` 模式处理以下请求，不得虚构实际使用过程：

$ARGUMENTS
