---
description: 使用 ARS-Grok 按已确认意见修订论文
argument-hint: 稿件、修改意见与作者决定
allowed-tools: [read_file, list_dir, grep, web_search, run_terminal_command, search_replace]
effort: medium
---

加载并遵循 `academic-research-suite` 根技能。读取 `ars/commands/ars-revision.md` 和 `ars/academic-paper/WORKFLOW.md`，以 `revision` 模式处理以下请求。不得替作者推断尚未确认的修改决定：

$ARGUMENTS
