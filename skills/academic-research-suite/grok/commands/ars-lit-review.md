---
description: 使用 ARS-Grok 执行文献综述或文献发现与综合
argument-hint: 研究问题、时间范围、数据库与交付格式
allowed-tools: [read_file, list_dir, grep, web_search, run_terminal_command, search_replace]
effort: medium
---

加载并遵循 `academic-research-suite` 根技能。读取 `ars/commands/ars-lit-review.md`。已有文献材料的写作任务进入 `academic-paper` 的 `lit-review` 模式；需要发现和综合资料时进入 `deep-research` 的 `lit-review` 模式。请求如下：

$ARGUMENTS
