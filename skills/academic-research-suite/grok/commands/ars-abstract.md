---
description: 使用 ARS-Grok 起草或修订论文摘要
argument-hint: 论文材料、摘要语言与期刊要求
allowed-tools: [read_file, list_dir, grep, web_search, run_terminal_command, search_replace]
---

加载并遵循 `academic-research-suite` 根技能。读取 `ars/commands/ars-abstract.md` 和 `ars/academic-paper/WORKFLOW.md`，以 `abstract-only` 模式处理以下请求：

$ARGUMENTS
