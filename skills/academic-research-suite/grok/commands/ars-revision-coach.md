---
description: 使用 ARS-Grok 形成非强制、可追踪的论文修订路线
argument-hint: 稿件与修改意见路径
allowed-tools: [read_file, list_dir, grep, web_search, search_replace]
---

加载并遵循 `academic-research-suite` 根技能。读取 `ars/commands/ars-revision-coach.md` 和 `ars/academic-paper/WORKFLOW.md`，以 `revision-coach` 模式处理以下请求。修订路线必须等待作者裁决：

$ARGUMENTS
