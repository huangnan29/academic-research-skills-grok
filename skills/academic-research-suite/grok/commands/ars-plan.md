---
description: 使用 ARS-Grok 制定论文或研究写作计划
argument-hint: 研究问题、材料路径与输出要求
allowed-tools: [read_file, list_dir, grep, web_search, run_terminal_command, search_replace]
---

加载并遵循 `academic-research-suite` 根技能。读取 `ars/commands/ars-plan.md` 和 `ars/academic-paper/WORKFLOW.md`，以 `plan` 模式处理以下请求：

$ARGUMENTS
